{
  "name": "@yaywind/modules",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && pnpm run build:react && pnpm run build:components",
    "build:react": "vite build --config ./vite.react.config.ts",
    "build:components": "vite build --config ./vite.components.config.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@yaywind/configs": "workspace:*",
    "@yaywind/components": "workspace:*"
  }
}
